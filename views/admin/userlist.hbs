HOLA LISTA 
{{#each usuario}}
      <h1>Perfil de Usuario</h1>
    <img src="{{foto}}" alt="Foto de perfil">
    <p>Nombre: {{nombre}}</p>
    <p>Nombre de usuario: {{username}}</p>
    <p>Correo electrónico: {{email}}</p>
    <p>Género: {{genero}}</p>
    <p>Nacionalidad: {{nacionalidad}}</p>
    <p>Rol: {{role}}</p>
    </div>
    <a href="/admin/dashboard/userList/edit/{{_id}}"><button>Editar</button></a>
     <a href="/admin/dashboard/userList/delete/{{_id}}"><button>Eliminar</button></a>
    {{/each}}

    <a href="/admin/dashboard"><button>Dashboard</button></a>